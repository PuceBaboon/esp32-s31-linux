# Standard nl80211 control socket used by wpa_cli and future esp32-config.
ctrl_interface=/run/wpa_supplicant
update_config=1
ap_scan=1

# Deliberately contains no network block. Add one with wpa_passphrase or
# wpa_cli; never ship an open-network fallback or a board-specific credential.
